[build-system]
requires = ["setuptools>=78", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tool-nikku696969-learntube-study-transcript-ujzngt7x"
version = "1.0.4"
description = "LearnTube AI Executa for retrieving public YouTube captions."
requires-python = ">=3.10"
dependencies = [
  "youtube-transcript-api>=1.2.2,<2",
]

[project.scripts]
"tool-nikku696969-learntube-study-transcript-ujzngt7x" = "my_first_anna_app_plugin:main"

[tool.setuptools]
py-modules = ["my_first_anna_app_plugin"]
